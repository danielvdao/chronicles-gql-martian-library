class ApplicationController < ActionController::Base
  def current_user
    # TODO: Stub out current user
    # token = request.headers["Authorization"].to_s
    # email = Base64.decode64(token)
    # User.find_by(email: email)
  end
end
